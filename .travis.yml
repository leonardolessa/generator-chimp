language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chimp' ]; then cd .. && eval "mv $currentfolder generator-chimp" && cd generator-chimp; fi

